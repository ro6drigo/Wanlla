@model Modelo.receta
@{
    ViewBag.Title = "Detalle Receta";
}

<ol class="breadcrumb">
    <li><a href="~/Admin/Receta/">Recetas</a></li>
    <li class="active">@Model.nom_receta</li>
</ol>

<div class="panel panel-success">
    <div class="panel-heading">
        Información de la Receta
    </div>
    <div>
        <dl class="dl-horizontal">
            <dt>ID</dt>
            <dd>@Model.id_receta</dd>
            <dt>NOMBRE</dt>
            <dd>@Model.nom_receta</dd>
            <dt>DESCRIPCIÓN</dt>
            <dd>@Model.des_receta</dd>
            <dt>IMAGEN</dt>
            <dd><img class="img-responsive thumbnail" src="~/images/@Model.img_receta" width="15%" height="15%"></dd>
        </dl>
    </div>
</div>

<div class="panel panel-success">
    <div class="panel-heading">
        Ingredientes Receta
    </div>
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th class="text-center">Ingrediente</th>
                    <th class="text-center">Cantidad</th>
                    <th class="text-center">Unidad Medida</th>
                    <th class="text-center">Acción</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var i in Model.ingrediente_receta)
                {
                    <tr>
                        <td>@(i.ingrediente.nom_ingrediente)</td>
                        <td>@(i.cant_ingrediente)</td>
                        <td>@(i.umed_ingrediente)</td>
                        <td><a href="~/Admin/IngredienteReceta/Mantenimiento/@i.id_receta" data-toggle="tooltip" data-placement="bottom" title="Editar"><span class="fa fa-pencil os-ac-icon os-hover-green"></span></a></td>
                        <td><a onclick="return confirm('¿Está seguro de eliminar el registro?');" href="~/Admin/IngredienteReceta/Eliminar/@i.id_receta" data-toggle="tooltip" data-placement="bottom" title="Eliminar"><span class="fa fa-trash os-ac-icon os-hover-red"></span></a></td>
                    </tr>
                }
            </tbody>


        </table>
    </div>
</div>
<div class="panel panel-success">
    <div class="panel-heading">
        Pasos Receta
    </div>
    <div class="table-responsive">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th class="text-center">Id</th>
                    <th class="text-center">Descripción</th>
                    <th class="text-center">Imagen</th>
                    <th class="text-center">Acción</th>
                </tr>
            </thead>
            <tbody>
                @{ int count = 1;}
                @foreach (var p in Model.paso_receta)
                {
                    <tr>
                        <th scope="row">@(count++)</th>
                        <td>@(p.des_paso)</td>
                        <td>@(p.img_paso != null ? "Si" : "No" )</td>
                        <td><a href="~/Admin/PasoReceta/Mantenimiento/@p.id_receta" data-toggle="tooltip" data-placement="bottom" title="Editar"><span class="fa fa-pencil os-ac-icon os-hover-green"></span></a></td>
                        <td><a onclick="return confirm('¿Está seguro de eliminar el registro?');" href="~/Admin/PasoReceta/Eliminar/@p.id_receta" data-toggle="tooltip" data-placement="bottom" title="Eliminar"><span class="fa fa-trash os-ac-icon os-hover-red"></span></a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
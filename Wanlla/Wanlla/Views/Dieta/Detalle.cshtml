@model Modelo.dieta
@{
    ViewBag.Title = Model.nom_dieta;
}

<div class="row">
    <div class="col-sm-6">
        @*@using (Html.BeginForm("Index", "Dieta", FormMethod.Post, new { @class = "form-inline" }))
        {
            <div class="form-group">
                <label class="sr-only" for="buscar">Buscar</label>
                <div class="input-group">
                    @Html.TextBox("buscar", null, new { type = "search", @class = "form-control", placeholder = "Ingrese nombre", id = "buscar", autocomplete = "off" })
                    <div class="input-group-addon"><a href="javascript: document.myform.submit();"><span class="fa fa-search"></span></a></div>
                </div>
            </div>
        }*@
    </div>
    <div class="col-sm-6 text-right">
        <a class="btn btn-default" href="~/Detalle/Mantenimiento" role="button">Agregar Receta</a>
    </div>
</div>

<br />

<div class="row">
    <div class="col-sm-12">
        @{ int count = 1;}
        @foreach (var r in Model.dieta_receta)
        {
            <div class="panel panel-success">
                <div class="panel-heading">
                    <h3 class="panel-title">Día @(count++)</h3>
                </div>
                <div class="panel-body">
                    <div class="media">
                        <div class="media-left">
                            <a href="#">
                                <img class="media-object" src="~/images/media1.jpg" alt="@(r.receta.nom_receta)">
                            </a>
                        </div>
                        <div class="media-body">
                            <h4 class="media-heading">@(r.receta.nom_receta)</h4>
                            @(r.receta.des_receta)
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>